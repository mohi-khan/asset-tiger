import Dispose from '@/components/dashboard/assets/dispose/dispose';
import React from 'react';

const DisposePage = () => {
    return (
        <div>
            <Dispose />
        </div>
    );
};

export default DisposePage;